#include<stdio.h>
#define SIZE 10000
int main()
{
    int A[SIZE],i;
    int N;
    scanf("%d\n",&N);
    for(i=0;i<N;i++)
    {
      scanf("%d ",&A[i]);
    }
    for(i=N-1;i>=0;i--)
    {
      printf("%d ",A[i]);
    }
 return 0;
}
