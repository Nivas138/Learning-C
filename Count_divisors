#include<stdio.h>
int main()
{
 int i,count=0,l,r,k;
 scanf("%d",&l);
 scanf("%d",&r);
 scanf("%d",&k);
 for(i=l;i<=r;i++)
 {
     if(i%k==0)
     {
         count+=1;
     }
 }
 printf("%d",count);
 return 0;
}
