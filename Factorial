#include<stdio.h>
//int f=1;
int fact(int n)
{
    if(n>=1)
    {
    return (n*fact(n-1));
    }
    else
      return 1;
}
int main()
{
    int i,N;
    scanf("%d",&N);
    i=fact(N);
    printf("%d",i);
  return 0;
}
